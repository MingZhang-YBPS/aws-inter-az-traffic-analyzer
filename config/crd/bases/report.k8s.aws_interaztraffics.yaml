---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: interaztraffics.report.k8s.aws
spec:
  group: report.k8s.aws
  names:
    kind: InterAZTraffic
    listKind: InterAZTrafficList
    plural: interaztraffics
    singular: interaztraffic
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InterAZTraffic is the Schema for the interaztraffics API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InterAZTrafficSpec defines the desired state of InterAZTraffic.
            properties:
              endTo:
                format: date-time
                type: string
                x-kubernetes-validations:
                - message: spec.endTo is immutable
                  rule: self == oldSelf
              startFrom:
                format: date-time
                type: string
                x-kubernetes-validations:
                  - message: spec.endTo is immutable
                    rule: self == oldSelf
              vpcId:
                description: |-
                  VPCId is the vpc id where this EKS cluster exists
                  Currently, I don't find anyway to retrieve vpc ID from EKS pod, so have to pass it from outside
                type: string
                x-kubernetes-validations:
                  - message: spec.endTo is immutable
                    rule: self == oldSelf
            required:
            - vpcId
            type: object
          status:
            description: InterAZTrafficStatus defines the observed state of InterAZTraffic.
            properties:
              lastestReportCreationTimeStamp:
                format: date-time
                type: string
              lastestReportLocation:
                description: LastestReportLocation is the latest report location in
                  S3, like "s3://the-bucket-name/the-object-name"
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
